{
  "name": "with-session",
  "private": true,
  "scripts": {
    "build": "rimraf dist && tsc",
    "start": "node dist/scripts/serve.js"
  },
  "devDependencies": {
    "rimraf": "^2.6.3",
    "typescript": "^3.4.5"
  },
  "dependencies": {
    "prismy": "^0.10.0",
    "prismy-cookie": "^0.3.0",
    "prismy-session": "0.0.1-3",
    "prismy-session-strategy-signed-cookie": "0.0.1-1"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/dist/"
    ]
  }
}
