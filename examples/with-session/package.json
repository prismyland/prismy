{
  "name": "with-session",
  "private": true,
  "scripts": {
    "build": "rimraf dist && tsc",
    "start": "node dist/scripts/serve.js"
  },
  "devDependencies": {
    "rimraf": "^2.6.3",
    "typescript": "^3.4.5"
  },
  "dependencies": {
    "prismy": "^1.3.0",
    "prismy-cookie": "^1.0.1",
    "prismy-method-router": "^1.0.0",
    "prismy-session": "^1.0.1",
    "prismy-session-strategy-jwt-cookie": "^1.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/dist/"
    ]
  }
}
